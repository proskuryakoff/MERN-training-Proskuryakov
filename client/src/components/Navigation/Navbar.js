import classes from './Navbar.css'

const navbar = (props) => (
    <header className={classes.Navbar}>
        <div>Menu</div>
        <div>Logo</div>
        <nav>...</nav>
    </header>
)

export default navbar;